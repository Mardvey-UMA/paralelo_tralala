Определить ускорение (т.е. отношение времени выполнения последовательной версии программы) на Вашей вычислительной системе при вычислении среднего значения функции f(x, y, z) , f : R^3 -> R, в единичной кубе с вершиной в начале координат и ребрами направленными вдоль координатных осей
A = h^3 СУММА(от i = 0 до N - 1)СУММА(от j = 0 до N - 1)СУММА(от k = 0 до N - 1) f(h(i + 1/2), h(j + 1/2), h(k + 1/2))
h = 1/2
Для N = 1000, 1500
Использовать балансировку нагрузки:
a) по умолчанию 
#pragma omp parallel for reduction(+: <имя переменной>)
b) статическую
#pragma omp parallel for reduction(+: <имя переменной>) schedule(static)
c) динамическую 
#pragma omp parallel for reduction(+: <имя переменной>) schedule(dynamic)

У меня три разные функции f
4) f(x, y, z) = 1 / (2 + e^(-z) * sin(x^2 - y^2))
6) f(x, y, z) = cos(x) / 1 + e^(x^2 + y^2 + z^2)
8) f(x, y, z) = 1 / (2 + e^(-x^2) * sin(y^2 + z^2))

2)То же самое для конуса единичной
высоты, ось которого совпадает с осью x, а основание – единичный круг в плоскости Oyz, с центром в начале координат
B = h^3(СУММА(от i = 0 до N) СУММА(от j = -(N - i) до N - i) СУММА(от k = -sqrt((N - i)^2 - j^2) до sqrt((N - i)^2 - j^2))) f (hi, hj, hk)
h = 1/2

